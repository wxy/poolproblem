# M1 Core + M2 CLI 实现计划（The Pool Problem）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 The Pool Problem 的可测试基础层：SwiftPM 核心库 `DiskReservoirCore`（配方、扫描、诚实计量、快照、流速/归因/预测、清理引擎）与命令行 `poolproblem`（scan/suggest/clean/status，JSON 输出）。

**Architecture:** 无 UI 依赖的 Swift Package 核心库 + swift-argument-parser 编写的可执行 target。核心库提供全部领域逻辑（蓄水池模型的扫描与流量分析），CLI 只是 JSON 外壳。App（M3）、小组件（M4）、打包（M5）在后续独立计划中实现，直接复用本计划产出的 Core。

**Tech Stack:** Swift 6、SwiftPM（swift-tools-version 6.0）、macOS 14+、Swift Testing（`import Testing`）、swift-argument-parser ≥ 1.5、Foundation。

## Global Constraints

- 平台最低版本：macOS 14（`Package.swift` 中 `platforms: [.macOS(.v14)]`）。
- 工具链：Xcode 16+ / Swift 6；测试用 Swift Testing，不用 XCTest。
- **Core 禁止依赖 AppKit/UIKit**：进程检测走 `/usr/bin/pgrep`（通过 `ProcessInspecting` 协议抽象，测试注入假实现）。
- 诚实计量：每个扫描项同时报告 `sizeBytes`（表观）、`allocatedBytes`（物理分配）、`reclaimableBytes`（inode 去重后的可真实释放量）；克隆感知实现见 Task 6。
- 数据目录解析顺序：环境变量 `POOLPROBLEM_DATA_DIR` → App Group 容器（`~/Library/Group Containers/group.com.poolproblem.app`）→ `~/Library/Application Support/PoolProblem`。测试一律用 `POOLPROBLEM_DATA_DIR` 指向临时目录。
- JSON schema 稳定：字段名使用 Swift `Codable` 默认 camelCase；顶层对象含 `"version": 1` 与 `"timestamp"`；任何破坏性变更必须提升 version。
- 配置默认值（来自设计文档）：水线 30GB；增长警报阈值 absoluteBytes=2GB 或（relative=50% 且 minimumAbsoluteBytes=500MB）；XCTestDevices 3 天、DerivedData 7 天、缓存 30 天；静默清理跳过最近 24h 修改过的文件。
- 提交规范：conventional commits（`feat:`/`fix:`/`test:`/`docs:`/`chore:`）；每个任务结束时必须 `swift test` 全绿才能提交。
- 首次构建需要网络拉取 swift-argument-parser；之后离线可构建。

---

## 文件结构总览

```
poolproblem/
├── Package.swift
├── .gitignore
├── README.md
├── Sources/
│   ├── DiskReservoirCore/
│   │   ├── Models/
│   │   │   ├── SafetyLevel.swift
│   │   │   ├── Category.swift
│   │   │   ├── CleanDisposition.swift
│   │   │   ├── ScanItem.swift
│   │   │   ├── Recipe.swift
│   │   │   ├── VolumeInfo.swift
│   │   │   ├── Snapshot.swift
│   │   │   ├── CleanLogEntry.swift
│   │   │   ├── CleanRule.swift
│   │   │   └── Config.swift
│   │   ├── Recipes/
│   │   │   ├── RecipeRegistry.swift
│   │   │   └── BuiltInRecipes.swift
│   │   ├── Scanner/
│   │   │   ├── VolumeReader.swift
│   │   │   ├── FileRecord.swift
│   │   │   ├── ReclaimableEstimator.swift
│   │   │   └── Scanner.swift
│   │   ├── Snapshot/
│   │   │   ├── SnapshotStore.swift
│   │   │   └── CleanLogStore.swift
│   │   ├── Flow/
│   │   │   ├── FlowAnalyzer.swift
│   │   │   └── FullPrediction.swift
│   │   ├── Cleaner/
│   │   │   ├── ProcessInspecting.swift
│   │   │   ├── RuleEvaluator.swift
│   │   │   ├── FileDeleting.swift
│   │   │   └── Cleaner.swift
│   │   └── Storage/
│   │       ├── StoragePaths.swift
│   │       └── JSONStore.swift
│   └── poolproblem/
│       ├── PoolProblemCLI.swift
│       ├── JSONOutput.swift
│       └── Commands/
│           ├── ScanCommand.swift
│           ├── SuggestCommand.swift
│           ├── CleanCommand.swift
│           └── StatusCommand.swift
├── Tests/
│   ├── DiskReservoirCoreTests/
│   │   ├── ModelsTests.swift
│   │   ├── RecipeRegistryTests.swift
│   │   ├── StoragePathsTests.swift
│   │   ├── ScannerTests.swift
│   │   ├── ReclaimableEstimatorTests.swift
│   │   ├── SnapshotStoreTests.swift
│   │   ├── FlowAnalyzerTests.swift
│   │   ├── FullPredictionTests.swift
│   │   ├── RuleEvaluatorTests.swift
│   │   ├── CleanerTests.swift
│   │   └── Fixtures.swift
│   └── PoolProblemCLITests/
│       └── CLIEndToEndTests.swift
└── docs/superpowers/plans/
    └── 2026-08-09-core-cli.md
```

---

### Task 1: SwiftPM 骨架

**Files:**
- Create: `Package.swift`
- Create: `.gitignore`
- Create: `Sources/DiskReservoirCore/Version.swift`
- Create: `Sources/poolproblem/PoolProblemCLI.swift`
- Create: `Tests/DiskReservoirCoreTests/SmokeTests.swift`

**Interfaces:**
- Consumes: 无。
- Produces: `DiskReservoirCore` 库 target（module 名 `DiskReservoirCore`）、`poolproblem` 可执行 product、两个测试 target。

- [ ] **Step 1: 写 Package.swift 与骨架文件**

```swift
// swift-tools-version:6.0
import PackageDescription

let package = Package(
    name: "poolproblem",
    platforms: [.macOS(.v14)],
    products: [
        .library(name: "DiskReservoirCore", targets: ["DiskReservoirCore"]),
        .executable(name: "poolproblem", targets: ["poolproblem"]),
    ],
    dependencies: [
        .package(url: "https://github.com/apple/swift-argument-parser.git", from: "1.5.0"),
    ],
    targets: [
        .target(name: "DiskReservoirCore"),
        .executableTarget(
            name: "poolproblem",
            dependencies: [
                "DiskReservoirCore",
                .product(name: "ArgumentParser", package: "swift-argument-parser"),
            ]
        ),
        .testTarget(name: "DiskReservoirCoreTests", dependencies: ["DiskReservoirCore"]),
        .testTarget(name: "PoolProblemCLITests", dependencies: ["poolproblem"]),
    ]
)
```

```gitignore
.build/
.swiftpm/
.DS_Store
*.xcodeproj
DerivedData/
```

`Sources/DiskReservoirCore/Version.swift`：

```swift
public enum DiskReservoirCore {
    public static let schemaVersion: Int = 1
}
```

`Sources/poolproblem/PoolProblemCLI.swift`：

```swift
import ArgumentParser

@main
struct PoolProblemCLI: ParsableCommand {
    static let configuration = CommandConfiguration(
        commandName: "poolproblem",
        abstract: "The Pool Problem - 磁盘蓄水池：扫描、预测、清理。",
        version: "0.1.0"
    )

    func run() throws {
        print("poolproblem 0.1.0")
    }
}
```

`Tests/DiskReservoirCoreTests/SmokeTests.swift`：

```swift
import Testing
@testable import DiskReservoirCore

@Test func schemaVersionIsOne() {
    #expect(DiskReservoirCore.schemaVersion == 1)
}
```

- [ ] **Step 2: 构建并跑测试**

Run: `swift test`

Expected: 构建成功；`SmokeTests` PASS。首次构建会联网拉取 swift-argument-parser。

- [ ] **Step 3: 提交**

```bash
git add Package.swift .gitignore Sources Tests
git commit -m "chore: scaffold SwiftPM package with core library and CLI"
```

---

### Task 2: 领域模型

**Files:**
- Create: `Sources/DiskReservoirCore/Models/SafetyLevel.swift`
- Create: `Sources/DiskReservoirCore/Models/Category.swift`
- Create: `Sources/DiskReservoirCore/Models/CleanDisposition.swift`
- Create: `Sources/DiskReservoirCore/Models/ScanItem.swift`
- Create: `Sources/DiskReservoirCore/Models/Recipe.swift`
- Create: `Sources/DiskReservoirCore/Models/VolumeInfo.swift`
- Create: `Sources/DiskReservoirCore/Models/Snapshot.swift`
- Create: `Sources/DiskReservoirCore/Models/CleanLogEntry.swift`
- Create: `Sources/DiskReservoirCore/Models/CleanRule.swift`
- Create: `Sources/DiskReservoirCore/Models/Config.swift`
- Test: `Tests/DiskReservoirCoreTests/ModelsTests.swift`

**Interfaces:**
- Consumes: 无。
- Produces: 以下公共类型（后续所有任务依赖）：

```swift
public enum SafetyLevel: String, Codable, CaseIterable, Sendable {
    case safeWhileRunning, requiresQuit, userConfirm
}

public enum Category: String, Codable, CaseIterable, Sendable {
    case xcode, simulator, packageManager, project, common, custom
}

public enum CleanDisposition: String, Codable, CaseIterable, Sendable {
    case trash, deletePermanently, none
}

public struct ScanItem: Codable, Equatable, Identifiable, Sendable {
    public let id: String
    public let recipeID: String
    public let name: String
    public let path: String
    public let category: Category
    public let safety: SafetyLevel
    public let disposition: CleanDisposition
    public let sizeBytes: Int64
    public let allocatedBytes: Int64
    public let reclaimableBytes: Int64
    public let fileCount: Int
    public let lastModified: Date?
}

public struct Recipe: Sendable {
    public let id: String
    public let name: String
    public let category: Category
    public let safety: SafetyLevel
    public let disposition: CleanDisposition
    public let defaultAgeDays: Int
    public let minimumSizeMB: Double
    public let processName: String?
    public let resolvePaths: @Sendable (StoragePaths) -> [String]
}

public struct VolumeInfo: Codable, Equatable, Sendable {
    public let totalBytes: Int64
    public let availableBytes: Int64
    public let timestamp: Date
}

public struct Snapshot: Codable, Equatable, Sendable {
    public let volume: VolumeInfo
    public let items: [ScanItem]
}

public struct CleanLogEntry: Codable, Equatable, Identifiable, Sendable {
    public let id: UUID
    public let timestamp: Date
    public let itemIDs: [String]
    public let freedBytes: Int64
    public let disposition: CleanDisposition
}

public struct CleanRule: Codable, Equatable, Sendable {
    public let recipeID: String
    public var enabled: Bool
    public var maxAgeDays: Int?
    public var minimumSizeMB: Double?
    public var disposition: CleanDisposition?
}

public struct Config: Codable, Equatable, Sendable {
    public var waterlineGB: Double
    public var rules: [CleanRule]
    public var whitelistPaths: [String]
    public var enabledRecipes: Set<String>
    public static let `default` = Config(
        waterlineGB: 30,
        rules: [],
        whitelistPaths: [],
        enabledRecipes: []
    )
}
```

- [ ] **Step 1: 写失败测试**

`Tests/DiskReservoirCoreTests/ModelsTests.swift`：

```swift
import Testing
@testable import DiskReservoirCore

@Test func scanItemCodableRoundTrip() throws {
    let item = ScanItem(
        id: "xctest-1", recipeID: "xctestdevices", name: "XCTestDevices",
        path: "/tmp/x", category: .xcode, safety: .safeWhileRunning,
        disposition: .deletePermanently, sizeBytes: 100, allocatedBytes: 80,
        reclaimableBytes: 10, fileCount: 3, lastModified: nil
    )
    let data = try JSONEncoder().encode(item)
    let decoded = try JSONDecoder().decode(ScanItem.self, from: data)
    #expect(decoded == item)
}

@Test func configDefaultWaterlineIs30GB() {
    #expect(Config.default.waterlineGB == 30)
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `swift test --filter ModelsTests`

Expected: FAIL —— `ScanItem`、`Config` 等类型不存在。

- [ ] **Step 3: 实现模型（按上方 Interfaces 代码块创建各文件）**

- [ ] **Step 4: 运行测试确认通过**

Run: `swift test --filter ModelsTests`

Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Models Tests/DiskReservoirCoreTests/ModelsTests.swift
git commit -m "feat: add core domain models"
```

---

### Task 3: 配方注册表

**Files:**
- Create: `Sources/DiskReservoirCore/Recipes/RecipeRegistry.swift`
- Create: `Sources/DiskReservoirCore/Recipes/BuiltInRecipes.swift`
- Test: `Tests/DiskReservoirCoreTests/RecipeRegistryTests.swift`

**Interfaces:**
- Consumes: `Recipe`、`StoragePaths`（Task 4 定义，本任务先用占位协议编译？——不，StoragePaths 在 Task 4 才实现。为避免循环，本任务先定义 `StoragePaths` 的最小可用版本：`public struct StoragePaths { public let homeDirectory: String }`，Task 4 扩充它。见 Task 4 说明）。
- Produces:

```swift
public struct RecipeRegistry {
    public static func builtIn() -> [Recipe]
}
```

- [ ] **Step 1: 定义 StoragePaths 最小版本 + 写失败测试**

在 `Sources/DiskReservoirCore/Storage/StoragePaths.swift` 先写入：

```swift
public struct StoragePaths: Sendable {
    public let homeDirectory: String
    public init(homeDirectory: String = NSHomeDirectory()) { self.homeDirectory = homeDirectory }
}
```

`Tests/DiskReservoirCoreTests/RecipeRegistryTests.swift`：

```swift
import Testing
@testable import DiskReservoirCore

@Test func builtInRecipesCoverCoreCategories() {
    let recipes = RecipeRegistry.builtIn()
    let ids = Set(recipes.map(\.id))
    #expect(ids.contains("xctestdevices"))
    #expect(ids.contains("deriveddata"))
    #expect(ids.contains("npm-cache"))
    #expect(ids.contains("uv-cache"))
    #expect(recipes.allSatisfy { !$0.id.isEmpty && !$0.name.isEmpty })
}

@Test func xctestdevicesRecipeResolvesToLibraryDeveloper() {
    let recipe = RecipeRegistry.builtIn().first { $0.id == "xctestdevices" }!
    let paths = StoragePaths(homeDirectory: "/Users/tester")
    let resolved = recipe.resolvePaths(paths)
    #expect(resolved == ["/Users/tester/Library/Developer/XCTestDevices"])
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `swift test --filter RecipeRegistryTests`

Expected: FAIL —— `RecipeRegistry` 不存在。

- [ ] **Step 3: 实现内置配方**

`BuiltInRecipes.swift` 返回以下配方（id、name、category、safety、disposition、defaultAgeDays、minimumSizeMB、processName、resolvePaths）：

| id | name | category | safety | disposition | age | processName |
|---|---|---|---|---|---|---|
| `xctestdevices` | XCTestDevices 测试快照 | xcode | safeWhileRunning | deletePermanently | 3 | nil |
| `deriveddata` | Xcode DerivedData | xcode | safeWhileRunning | deletePermanently | 7 | nil |
| `xcode-archives` | Xcode Archives | xcode | safeWhileRunning | trash | 30 | nil |
| `xcode-docscache` | Xcode DocumentationCache | xcode | safeWhileRunning | deletePermanently | 30 | nil |
| `core-simulator-devices` | 模拟器设备数据 | simulator | requiresQuit | trash | 30 | "Simulator" |
| `npm-cache` | npm 缓存 | packageManager | safeWhileRunning | deletePermanently | 30 | nil |
| `pnpm-store` | pnpm store | packageManager | safeWhileRunning | deletePermanently | 30 | nil |
| `uv-cache` | uv 缓存 | packageManager | safeWhileRunning | deletePermanently | 30 | nil |
| `cocoapods-cache` | CocoaPods 缓存 | packageManager | safeWhileRunning | deletePermanently | 30 | nil |
| `homebrew-cache` | Homebrew 缓存 | packageManager | safeWhileRunning | deletePermanently | 30 | nil |
| `library-caches` | 应用缓存 (~/Library/Caches) | common | safeWhileRunning | trash | 30 | nil |
| `trash` | 废纸篓 | common | safeWhileRunning | deletePermanently | 30 | nil |

路径解析（`resolvePaths`）基于 `StoragePaths.homeDirectory`，拼出：

```swift
// xctestdevices
paths.homeDirectory + "/Library/Developer/XCTestDevices"
// deriveddata
paths.homeDirectory + "/Library/Developer/Xcode/DerivedData"
// npm-cache
paths.homeDirectory + "/.npm"
// uv-cache
paths.homeDirectory + "/.cache/uv"
// library-caches
paths.homeDirectory + "/Library/Caches"
// trash
paths.homeDirectory + "/.Trash"
```

其余配方路径按表在 `BuiltInRecipes.swift` 中补齐（`xcode-archives` → `~/Library/Developer/Xcode/Archives`；`xcode-docscache` → `~/Library/Developer/Xcode/DocumentationCache`；`core-simulator-devices` → `~/Library/Developer/CoreSimulator/Devices`；`pnpm-store` → `~/Library/pnpm`；`cocoapods-cache` → `~/Library/Caches/CocoaPods`；`homebrew-cache` → `~/Library/Caches/Homebrew`）。

`RecipeRegistry.builtIn()` 返回上述数组。

- [ ] **Step 4: 运行测试确认通过**

Run: `swift test --filter RecipeRegistryTests`

Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Recipes Sources/DiskReservoirCore/Storage/StoragePaths.swift Tests/DiskReservoirCoreTests/RecipeRegistryTests.swift
git commit -m "feat: add built-in recipe registry"
```

---

### Task 4: 存储路径与 JSON 存储

**Files:**
- Modify: `Sources/DiskReservoirCore/Storage/StoragePaths.swift`
- Create: `Sources/DiskReservoirCore/Storage/JSONStore.swift`
- Test: `Tests/DiskReservoirCoreTests/StoragePathsTests.swift`

**Interfaces:**
- Consumes: 无。
- Produces:

```swift
public struct StoragePaths: Sendable {
    public let baseURL: URL
    public init(baseURL: URL? = nil)
    public var snapshotsURL: URL
    public var configURL: URL
    public var cleanLogURL: URL
}

public protocol JSONStoring: Sendable {
    func save<T: Encodable>(_ value: T, to url: URL) throws
    func load<T: Decodable>(_ type: T.Type, from url: URL) throws -> T?
}

public struct JSONStore: JSONStoring {
    public init(fileManager: FileManager = .default)
}
```

- [ ] **Step 1: 写失败测试**

`Tests/DiskReservoirCoreTests/StoragePathsTests.swift`：

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func dataDirRespectsEnvironmentOverride() {
    setenv("POOLPROBLEM_DATA_DIR", "/tmp/pp-test-data", 1)
    defer { unsetenv("POOLPROBLEM_DATA_DIR") }
    let paths = StoragePaths()
    #expect(paths.baseURL.path == "/tmp/pp-test-data")
    #expect(paths.snapshotsURL.lastPathComponent == "snapshots.json")
    #expect(paths.configURL.lastPathComponent == "config.json")
    #expect(paths.cleanLogURL.lastPathComponent == "clean-log.json")
}

@Test func jsonStoreRoundTrip() throws {
    let url = FileManager.default.temporaryDirectory.appendingPathComponent("pp-test-\(UUID().uuidString).json")
    defer { try? FileManager.default.removeItem(at: url) }
    let store = JSONStore()
    try store.save([1, 2, 3], to: url)
    let loaded: [Int]? = try store.load([Int].self, from: url)
    #expect(loaded == [1, 2, 3])
    let missing: [Int]? = try store.load([Int].self, from: url.appendingPathExtension("nope"))
    #expect(missing == nil)
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `swift test --filter StoragePathsTests`

Expected: FAIL —— `StoragePaths` 尚未有这些成员、`JSONStore` 不存在。

- [ ] **Step 3: 实现**

`StoragePaths.swift` 完整实现（`baseURL` 解析顺序：环境变量 `POOLPRODUCT_DATA_DIR` → App Group → Application Support；三个文件 URL 各自带 `schemaVersion` 目录，本版本直接放 base 下）：

```swift
public struct StoragePaths: Sendable {
    public let baseURL: URL

    public init(baseURL: URL? = nil) {
        if let baseURL { self.baseURL = baseURL }
        else if let env = ProcessInfo.processInfo.environment["POOLPROBLEM_DATA_DIR"] {
            self.baseURL = URL(fileURLWithPath: env, isDirectory: true)
        } else if let group = FileManager.default.containerURL(
            forSecurityApplicationGroupIdentifier: "group.com.poolproblem.app"
        ) {
            self.baseURL = group
        } else {
            self.baseURL = FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
                .appendingPathComponent("PoolProblem", isDirectory: true)
        }
    }

    public var snapshotsURL: URL { baseURL.appendingPathComponent("snapshots.json") }
    public var configURL: URL { baseURL.appendingPathComponent("config.json") }
    public var cleanLogURL: URL { baseURL.appendingPathComponent("clean-log.json") }
}
```

`JSONStore.swift`：

```swift
public struct JSONStore: JSONStoring {
    private let fileManager: FileManager
    private let encoder: JSONEncoder
    private let decoder: JSONDecoder

    public init(fileManager: FileManager = .default) {
        self.fileManager = fileManager
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        self.encoder = encoder
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        self.decoder = decoder
    }

    public func save<T: Encodable>(_ value: T, to url: URL) throws {
        try fileManager.createDirectory(at: url.deletingLastPathComponent(), withIntermediateDirectories: true)
        let data = try encoder.encode(value)
        try data.write(to: url, options: [.atomic])
    }

    public func load<T: Decodable>(_ type: T.Type, from url: URL) throws -> T? {
        guard fileManager.fileExists(atPath: url.path) else { return nil }
        let data = try Data(contentsOf: url)
        return try decoder.decode(T.self, from: data)
    }
}
```

注意：`StoragePaths` 同时要保留 `homeDirectory`（Task 3 的配方用）。为兼容，给 `StoragePaths` 增加 `public let homeDirectory: String`，默认 `NSHomeDirectory()`，`init(baseURL:homeDirectory:)` 双参数；Task 3 的调用 `StoragePaths(homeDirectory:)` 改为 `StoragePaths(baseURL: nil, homeDirectory: "/Users/tester")`。同步修改 `RecipeRegistryTests` 中的调用。

- [ ] **Step 4: 运行测试确认通过**

Run: `swift test --filter StoragePathsTests` 以及 `swift test --filter RecipeRegistryTests`

Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Storage Tests/DiskReservoirCoreTests/StoragePathsTests.swift
git commit -m "feat: add storage paths and JSON store"
```

---

### Task 5: 扫描器（大小/文件数/修改时间）

**Files:**
- Create: `Sources/DiskReservoirCore/Scanner/VolumeReader.swift`
- Create: `Sources/DiskReservoirCore/Scanner/FileRecord.swift`
- Create: `Sources/DiskReservoirCore/Scanner/Scanner.swift`
- Test: `Tests/DiskReservoirCoreTests/ScannerTests.swift`
- Test: `Tests/DiskReservoirCoreTests/Fixtures.swift`

**Interfaces:**
- Consumes: `ScanItem`、`Recipe`、`VolumeInfo`、`StoragePaths`、`RecipeRegistry`。
- Produces:

```swift
public struct FileRecord: Sendable {
    public let itemID: String
    public let url: URL
    public let allocatedBytes: Int64
    public let deviceID: Int32
    public let inode: UInt64
    public let lastModified: Date
}

public enum VolumeReader {
    public static func read(fileURL: URL, fileManager: FileManager = .default) -> VolumeInfo
}

public struct ScanResult: Sendable {
    public let volume: VolumeInfo
    public let items: [ScanItem]
    public let records: [FileRecord]
}

public struct Scanner: Sendable {
    public init(
        fileManager: FileManager = .default,
        now: @escaping () -> Date = Date.init,
        includeHidden: Bool = true
    )
    public func scan(recipes: [Recipe], homeDirectory: String) throws -> ScanResult
}
```

- [ ] **Step 1: 写测试与 fixture 工具**

`Fixtures.swift`：

```swift
import Foundation
@testable import DiskReservoirCore

enum Fixtures {
    /// 创建临时目录并写入给定大小的文件，返回目录 URL。
    static func makeTree(root: URL, files: [(relativePath: String, bytes: Int)]) throws {
        for file in files {
            let url = root.appendingPathComponent(file.relativePath)
            try FileManager.default.createDirectory(
                at: url.deletingLastPathComponent(),
                withIntermediateDirectories: true
            )
            let data = Data(repeating: 0xAB, count: file.bytes)
            try data.write(to: url)
        }
    }
}
```

`ScannerTests.swift`：

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func scannerReportsSizeAndFileCount() throws {
    let root = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-scan-\(UUID().uuidString)", isDirectory: true)
    try FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
    defer { try? FileManager.default.removeItem(at: root) }
    try Fixtures.makeTree(root: root, files: [("a.bin", 4096), ("sub/b.bin", 8192)])

    let recipe = Recipe(
        id: "fixture", name: "Fixture", category: .common, safety: .safeWhileRunning,
        disposition: .deletePermanently, defaultAgeDays: 30, minimumSizeMB: 0,
        processName: nil, resolvePaths: { _ in [root.path] }
    )
    let result = try Scanner().scan(recipes: [recipe], homeDirectory: root.path)
    let item = result.items.first { $0.recipeID == "fixture" }!
    #expect(item.fileCount == 2)
    #expect(item.sizeBytes > 0)
    #expect(item.allocatedBytes > 0)
    #expect(result.records.count == 2)
}

@Test func volumeReaderReturnsAvailableCapacity() {
    let info = VolumeReader.read(fileURL: URL(fileURLWithPath: "/"))
    #expect(info.totalBytes > 0)
    #expect(info.availableBytes > 0)
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `swift test --filter ScannerTests`

Expected: FAIL —— `Scanner`/`VolumeReader` 不存在。

- [ ] **Step 3: 实现**

`VolumeReader.swift`：

```swift
public enum VolumeReader {
    public static func read(fileURL: URL, fileManager: FileManager = .default) -> VolumeInfo {
        let keys: Set<URLResourceKey> = [
            .volumeTotalCapacityKey,
            .volumeAvailableCapacityForImportantUsageKey,
        ]
        let values = try? fileURL.resourceValues(forKeys: keys)
        return VolumeInfo(
            totalBytes: Int64(values?.volumeTotalCapacity ?? 0),
            availableBytes: Int64(values?.volumeAvailableCapacityForImportantUsage ?? 0),
            timestamp: Date()
        )
    }
}
```

`Scanner.swift` 核心逻辑（递归枚举，跳过符号链接，累计 allocated/size，收集 FileRecord）：

```swift
public struct Scanner: Sendable {
    private let fileManager: FileManager
    private let now: () -> Date
    private let includeHidden: Bool

    public init(fileManager: FileManager = .default, now: @escaping () -> Date = Date.init, includeHidden: Bool = true) {
        self.fileManager = fileManager
        self.now = now
        self.includeHidden = includeHidden
    }

    public func scan(recipes: [Recipe], homeDirectory: String) throws -> ScanResult {
        var items: [ScanItem] = []
        var records: [FileRecord] = []
        for recipe in recipes {
            for path in recipe.resolvePaths(StoragePaths(baseURL: nil, homeDirectory: homeDirectory)) {
                guard fileManager.fileExists(atPath: path) else { continue }
                let url = URL(fileURLWithPath: path, isDirectory: true)
                let (size, allocated, count, modified, files) = try measureDirectory(url, itemID: recipe.id)
                items.append(ScanItem(
                    id: "\(recipe.id):\(path)", recipeID: recipe.id, name: recipe.name,
                    path: path, category: recipe.category, safety: recipe.safety,
                    disposition: recipe.disposition, sizeBytes: size, allocatedBytes: allocated,
                    reclaimableBytes: allocated, fileCount: count, lastModified: modified
                ))
                records.append(contentsOf: files)
            }
        }
        let volume = VolumeReader.read(fileURL: URL(fileURLWithPath: homeDirectory), fileManager: fileManager)
        return ScanResult(volume: volume, items: items, records: records)
    }

    private func measureDirectory(
        _ url: URL, itemID: String
    ) throws -> (size: Int64, allocated: Int64, count: Int, modified: Date?, files: [FileRecord]) {
        let keys: Set<URLResourceKey> = [
            .isRegularFileKey, .isDirectoryKey, .isSymbolicLinkKey,
            .fileSizeKey, .totalFileAllocatedSizeKey, .contentModificationDateKey,
            .fileResourceIdentifierKey,
        ]
        let enumerator = fileManager.enumerator(
            at: url, includingPropertiesForKeys: Array(keys),
            options: includeHidden ? [] : [.skipsHiddenFiles]
        )
        var size: Int64 = 0
        var allocated: Int64 = 0
        var count = 0
        var newest: Date?
        var files: [FileRecord] = []
        while let element = enumerator?.nextObject() as? URL {
            let values = try element.resourceValues(forKeys: keys)
            if values.isSymbolicLink == true { continue }
            if values.isRegularFile == true {
                let alloc = Int64(values.totalFileAllocatedSize ?? values.fileSize ?? 0)
                size += Int64(values.fileSize ?? 0)
                allocated += alloc
                count += 1
                let stat = try element.withUnsafeFileSystemRepresentation { ptr in
                    var st = stat()
                    ptr.map { stat($0, &st) }
                    return st
                }
                files.append(FileRecord(
                    itemID: itemID, url: element, allocatedBytes: alloc,
                    deviceID: stat.st_dev, inode: stat.st_ino,
                    lastModified: values.contentModificationDate ?? .distantPast
                ))
                if let date = values.contentModificationDate, date > (newest ?? .distantPast) {
                    newest = date
                }
            }
        }
        return (size, allocated, count, newest, files)
    }
}
```

`st_dev`/`st_ino` 为 `Int32`/`UInt64`，需要导入 `Darwin`（Foundation 已包含）。`withUnsafeFileSystemRepresentation` 返回可选指针，闭包内 `ptr.map { stat($0, &st) }` 后返回 `st`（默认 `stat()` 已初始化）。

- [ ] **Step 4: 运行测试确认通过**

Run: `swift test --filter ScannerTests`

Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Scanner Tests/DiskReservoirCoreTests/ScannerTests.swift Tests/DiskReservoirCoreTests/Fixtures.swift
git commit -m "feat: add scanner with allocated size and file records"
```

---

### Task 6: 诚实计量（APFS 克隆去重）

**Files:**
- Create: `Sources/DiskReservoirCore/Scanner/ReclaimableEstimator.swift`
- Test: `Tests/DiskReservoirCoreTests/ReclaimableEstimatorTests.swift`

**Interfaces:**
- Consumes: `FileRecord`、`ScanItem`。
- Produces:

```swift
public struct ReclaimableEstimator: Sendable {
    public init()
    /// itemID → 可真实释放字节数。同一 (deviceID, inode) 出现在多个 item 中时，共享部分不算可释放。
    public func estimate(records: [FileRecord]) -> [String: Int64]
    /// 用 estimate 结果更新 ScanItem.reclaimableBytes
    public func apply(to items: [ScanItem], records: [FileRecord]) -> [ScanItem]
}
```

- [ ] **Step 1: 写失败测试**

`Tests/DiskReservoirCoreTests/ReclaimableEstimatorTests.swift`：

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func sharedInodeCountsOnce() {
    let shared = FileRecord(
        itemID: "a", url: URL(fileURLWithPath: "/tmp/a"), allocatedBytes: 1000,
        deviceID: 1, inode: 42, lastModified: .distantPast
    )
    let clone = FileRecord(
        itemID: "b", url: URL(fileURLWithPath: "/tmp/b"), allocatedBytes: 1000,
        deviceID: 1, inode: 42, lastModified: .distantPast
    )
    let uniqueB = FileRecord(
        itemID: "b", url: URL(fileURLWithPath: "/tmp/c"), allocatedBytes: 500,
        deviceID: 1, inode: 43, lastModified: .distantPast
    )
    let result = ReclaimableEstimator().estimate(records: [shared, clone, uniqueB])
    #expect(result["a"] == 0)      // 共享 inode：删 a 不释放
    #expect(result["b"] == 500)    // 只有独有 inode 可释放
}

@Test func realClonefileReducesReclaimable() throws {
    let root = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-clone-\(UUID().uuidString)", isDirectory: true)
    try FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
    defer { try? FileManager.default.removeItem(at: root) }
    let src = root.appendingPathComponent("src.bin")
    try Data(repeating: 1, count: 1_000_000).write(to: src)
    let dst = root.appendingPathComponent("clone.bin")
    clonefile(src.path, dst.path, 0)

    let records = [
        FileRecord(itemID: "a", url: src, allocatedBytes: 1_000_000, deviceID: 1, inode: 100, lastModified: .distantPast),
        FileRecord(itemID: "a", url: dst, allocatedBytes: 1_000_000, deviceID: 1, inode: 100, lastModified: .distantPast),
    ]
    let result = ReclaimableEstimator().estimate(records: records)
    #expect(result["a"] == 0)
}
```

`clonefile` 需要 `import Darwin`。

- [ ] **Step 2: 运行测试确认失败**

Run: `swift test --filter ReclaimableEstimatorTests`

Expected: FAIL —— 类型不存在；且真实克隆测试会失败（inode 相同导致可释放被算成 2MB）。

- [ ] **Step 3: 实现**

`ReclaimableEstimator.swift`：

```swift
public struct ReclaimableEstimator: Sendable {
    public init() {}

    public func estimate(records: [FileRecord]) -> [String: Int64] {
        var inodeOwners: [InodeKey: (Int64, Set<String>)] = [:]
        for record in records {
            let key = InodeKey(device: record.deviceID, inode: record.inode)
            var entry = inodeOwners[key] ?? (0, [])
            entry.0 = max(entry.0, record.allocatedBytes)
            entry.1.insert(record.itemID)
            inodeOwners[key] = entry
        }
        var result: [String: Int64] = [:]
        for (_, (bytes, owners)) in inodeOwners where owners.count == 1 {
            let owner = owners.first!
            result[owner, default: 0] += bytes
        }
        return result
    }

    public func apply(to items: [ScanItem], records: [FileRecord]) -> [ScanItem] {
        let estimates = estimate(records: records)
        return items.map { item in
            ScanItem(
                id: item.id, recipeID: item.recipeID, name: item.name, path: item.path,
                category: item.category, safety: item.safety, disposition: item.disposition,
                sizeBytes: item.sizeBytes, allocatedBytes: item.allocatedBytes,
                reclaimableBytes: estimates[item.id] ?? 0,
                fileCount: item.fileCount, lastModified: item.lastModified
            )
        }
    }

    private struct InodeKey: Hashable {
        let device: Int32
        let inode: UInt64
    }
}
```

同时修改 `Scanner.scan`：`items` 在返回前经过 `ReclaimableEstimator().apply(to:records:)`，使 `reclaimableBytes` 为去重后数值。

- [ ] **Step 4: 运行测试确认通过**

Run: `swift test --filter ReclaimableEstimatorTests` 及 `swift test --filter ScannerTests`

Expected: PASS（clonefile 测试在 APFS 上验证真实去重；非 APFS 文件系统该测试标注 `.disabled` 以 `if` 判断 `statfs` 文件系统类型为 APFS 时跳过）。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Scanner/ReclaimableEstimator.swift Tests/DiskReservoirCoreTests/ReclaimableEstimatorTests.swift Sources/DiskReservoirCore/Scanner/Scanner.swift
git commit -m "feat: honest accounting with inode-based dedup (APFS clone aware)"
```

---

### Task 7: 快照存储

**Files:**
- Create: `Sources/DiskReservoirCore/Snapshot/SnapshotStore.swift`
- Test: `Tests/DiskReservoirCoreTests/SnapshotStoreTests.swift`

**Interfaces:**
- Consumes: `Snapshot`、`StoragePaths`、`JSONStoring`。
- Produces:

```swift
public struct SnapshotStore: Sendable {
    public init(paths: StoragePaths, store: JSONStoring = JSONStore())
    public func append(_ snapshot: Snapshot) throws
    public func snapshots() throws -> [Snapshot]
    public func prune(retainingDays: Int = 90) throws
}
```

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func snapshotStoreAppendsAndPrunes() throws {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-store-\(UUID().uuidString)", isDirectory: true)
    defer { try? FileManager.default.removeItem(at: dir) }
    let paths = StoragePaths(baseURL: dir)
    let store = SnapshotStore(paths: paths)
    let now = Date()
    let old = Snapshot(volume: VolumeInfo(totalBytes: 100, availableBytes: 90, timestamp: now.addingTimeInterval(-100 * 86_400)), items: [])
    let fresh = Snapshot(volume: VolumeInfo(totalBytes: 100, availableBytes: 95, timestamp: now), items: [])
    try store.append(old)
    try store.append(fresh)
    #expect(try store.snapshots().count == 2)
    try store.prune(retainingDays: 30)
    #expect(try store.snapshots().count == 1)
    #expect(try store.snapshots().first?.volume.availableBytes == 95)
}
```

- [ ] **Step 2: 运行测试确认失败** —— `swift test --filter SnapshotStoreTests`，Expected: FAIL。

- [ ] **Step 3: 实现**

```swift
public struct SnapshotStore: Sendable {
    private let paths: StoragePaths
    private let store: JSONStoring

    public init(paths: StoragePaths, store: JSONStoring = JSONStore()) {
        self.paths = paths
        self.store = store
    }

    public func append(_ snapshot: Snapshot) throws {
        var all = try snapshots()
        all.append(snapshot)
        all.sort { $0.volume.timestamp < $1.volume.timestamp }
        try store.save(all, to: paths.snapshotsURL)
    }

    public func snapshots() throws -> [Snapshot] {
        try store.load([Snapshot].self, from: paths.snapshotsURL) ?? []
    }

    public func prune(retainingDays: Int = 90) throws {
        let cutoff = Date().addingTimeInterval(-Double(retainingDays) * 86_400)
        let kept = try snapshots().filter { $0.volume.timestamp >= cutoff }
        try store.save(kept, to: paths.snapshotsURL)
    }
}
```

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Snapshot/SnapshotStore.swift Tests/DiskReservoirCoreTests/SnapshotStoreTests.swift
git commit -m "feat: add snapshot store with pruning"
```

---

### Task 8: 流量分析（归因/回涨/增长警报）

**Files:**
- Create: `Sources/DiskReservoirCore/Flow/FlowAnalyzer.swift`
- Test: `Tests/DiskReservoirCoreTests/FlowAnalyzerTests.swift`

**Interfaces:**
- Consumes: `Snapshot`、`CleanLogEntry`、`ScanItem`。
- Produces:

```swift
public struct AttributionReport: Codable, Equatable, Sendable {
    public let windowDays: Int
    public let categoryDeltas: [Category: Int64]
    public let recipeDeltas: [String: Int64]
    public let totalDelta: Int64
}

public struct RegrowthReport: Codable, Equatable, Sendable {
    public let recipeID: String
    public let itemID: String
    public let name: String
    public let cleanedBytes: Int64
    public let regrownBytes: Int64
}

public struct GrowthThresholds: Equatable, Sendable {
    public var absoluteBytes: Int64
    public var relativePercent: Double
    public var minimumAbsoluteBytes: Int64
    public static let `default` = GrowthThresholds(
        absoluteBytes: 2 << 30, relativePercent: 0.5, minimumAbsoluteBytes: 500 << 20
    )
}

public struct GrowthAlert: Equatable, Sendable {
    public let itemID: String
    public let name: String
    public let deltaBytes: Int64
}

public struct FlowAnalyzer: Sendable {
    public init()
    public func attribution(snapshots: [Snapshot], windowDays: Int = 7) -> AttributionReport
    public func regrowth(snapshots: [Snapshot], log: [CleanLogEntry]) -> [RegrowthReport]
    public func growthAlert(snapshots: [Snapshot], thresholds: GrowthThresholds = .default) -> GrowthAlert?
}
```

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

private func item(_ id: String, recipe: String, name: String, category: Category, size: Int64) -> ScanItem {
    ScanItem(
        id: id, recipeID: recipe, name: name, path: "/tmp/\(id)",
        category: category, safety: .safeWhileRunning, disposition: .deletePermanently,
        sizeBytes: size, allocatedBytes: size, reclaimableBytes: size,
        fileCount: 1, lastModified: nil
    )
}

@Test func attributionComputesDeltas() {
    let earlier = Snapshot(
        volume: VolumeInfo(totalBytes: 1000, availableBytes: 800, timestamp: Date().addingTimeInterval(-86_400)),
        items: [item("x1", recipe: "xctestdevices", name: "XCTest", category: .xcode, size: 100)]
    )
    let later = Snapshot(
        volume: VolumeInfo(totalBytes: 1000, availableBytes: 700, timestamp: Date()),
        items: [item("x1", recipe: "xctestdevices", name: "XCTest", category: .xcode, size: 250)]
    )
    let report = FlowAnalyzer().attribution(snapshots: [earlier, later])
    #expect(report.recipeDeltas["xctestdevices"] == 150)
    #expect(report.categoryDeltas[.xcode] == 150)
    #expect(report.totalDelta == 150)
}

@Test func growthAlertTriggersOnAbsoluteGrowth() {
    let earlier = Snapshot(
        volume: VolumeInfo(totalBytes: 1000, availableBytes: 800, timestamp: Date().addingTimeInterval(-86_400)),
        items: [item("x1", recipe: "r", name: "N", category: .xcode, size: 100)]
    )
    let later = Snapshot(
        volume: VolumeInfo(totalBytes: 1000, availableBytes: 400, timestamp: Date()),
        items: [item("x1", recipe: "r", name: "N", category: .xcode, size: 3_000_000_000)]
    )
    let alert = FlowAnalyzer().growthAlert(snapshots: [earlier, later])
    #expect(alert?.itemID == "x1")
    #expect((alert?.deltaBytes ?? 0) > 2 << 30)
}

@Test func regrowthTracksCleanup() {
    let log = CleanLogEntry(
        id: UUID(), timestamp: Date().addingTimeInterval(-86_400),
        itemIDs: ["x1"], freedBytes: 500, disposition: .deletePermanently
    )
    let earlier = Snapshot(
        volume: VolumeInfo(totalBytes: 1000, availableBytes: 900, timestamp: Date().addingTimeInterval(-86_400)),
        items: []
    )
    let later = Snapshot(
        volume: VolumeInfo(totalBytes: 1000, availableBytes: 850, timestamp: Date()),
        items: [item("x1", recipe: "xctestdevices", name: "XCTest", category: .xcode, size: 300)]
    )
    let reports = FlowAnalyzer().regrowth(snapshots: [earlier, later], log: [log])
    #expect(reports.first?.cleanedBytes == 500)
    #expect(reports.first?.regrownBytes == 300)
}
```

- [ ] **Step 2: 运行测试确认失败** —— Expected: FAIL。

- [ ] **Step 3: 实现**

```swift
public struct FlowAnalyzer: Sendable {
    public init() {}

    public func attribution(snapshots: [Snapshot], windowDays: Int = 7) -> AttributionReport {
        let cutoff = Date().addingTimeInterval(-Double(windowDays) * 86_400)
        let inWindow = snapshots.filter { $0.volume.timestamp >= cutoff }
        guard let first = inWindow.first, let last = inWindow.last, first.volume.timestamp < last.volume.timestamp else {
            return AttributionReport(windowDays: windowDays, categoryDeltas: [:], recipeDeltas: [:], totalDelta: 0)
        }
        let firstByID = Dictionary(uniqueKeysWithValues: first.items.map { ($0.id, $0.sizeBytes) })
        let lastByID = Dictionary(uniqueKeysWithValues: last.items.map { ($0.id, $0.sizeBytes) })
        var recipeDeltas: [String: Int64] = [:]
        var categoryDeltas: [Category: Int64] = [:]
        for (id, oldSize) in firstByID {
            let newSize = lastByID[id] ?? 0
            let delta = newSize - oldSize
            if let item = last.items.first(where: { $0.id == id }) ?? first.items.first(where: { $0.id == id }) {
                recipeDeltas[item.recipeID, default: 0] += delta
                categoryDeltas[item.category, default: 0] += delta
            }
        }
        for (id, newSize) in lastByID where firstByID[id] == nil {
            if let item = last.items.first(where: { $0.id == id }) {
                recipeDeltas[item.recipeID, default: 0] += newSize
                categoryDeltas[item.category, default: 0] += newSize
            }
        }
        let total = recipeDeltas.values.reduce(0, +)
        return AttributionReport(
            windowDays: windowDays, categoryDeltas: categoryDeltas,
            recipeDeltas: recipeDeltas, totalDelta: total
        )
    }

    public func growthAlert(snapshots: [Snapshot], thresholds: GrowthThresholds = .default) -> GrowthAlert? {
        guard let first = snapshots.first, let last = snapshots.last,
              last.volume.timestamp.timeIntervalSince(first.volume.timestamp) >= 86_400 else { return nil }
        let firstByID = Dictionary(uniqueKeysWithValues: first.items.map { ($0.id, $0.sizeBytes) })
        for item in last.items {
            guard let old = firstByID[item.id] else { continue }
            let delta = item.sizeBytes - old
            let overAbsolute = delta > thresholds.absoluteBytes
            let overRelative = delta > Int64(Double(old) * thresholds.relativePercent) && delta > thresholds.minimumAbsoluteBytes
            if overAbsolute || overRelative {
                return GrowthAlert(itemID: item.id, name: item.name, deltaBytes: delta)
            }
        }
        return nil
    }

    public func regrowth(snapshots: [Snapshot], log: [CleanLogEntry]) -> [RegrowthReport] {
        guard let first = snapshots.first, let last = snapshots.last else { return [] }
        let firstByID = Dictionary(uniqueKeysWithValues: first.items.map { ($0.id, $0.sizeBytes) })
        let lastByID = Dictionary(uniqueKeysWithValues: last.items.map { ($0.id, $0.sizeBytes) })
        var result: [RegrowthReport] = []
        for entry in log {
            for itemID in entry.itemIDs {
                let cleaned = firstByID[itemID] ?? 0
                let regrown = lastByID[itemID] ?? 0
                if regrown > 0, let item = last.items.first(where: { $0.id == itemID }) {
                    result.append(RegrowthReport(
                        recipeID: item.recipeID, itemID: itemID, name: item.name,
                        cleanedBytes: cleaned, regrownBytes: regrown
                    ))
                }
            }
        }
        return result
    }
}
```

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Flow/FlowAnalyzer.swift Tests/DiskReservoirCoreTests/FlowAnalyzerTests.swift
git commit -m "feat: flow analysis with attribution, regrowth tracking and growth alerts"
```

---

### Task 9: 满盘预测

**Files:**
- Create: `Sources/DiskReservoirCore/Flow/FullPrediction.swift`
- Test: `Tests/DiskReservoirCoreTests/FullPredictionTests.swift`

**Interfaces:**
- Consumes: `Snapshot`。
- Produces:

```swift
public struct FullPrediction: Sendable {
    public init()
    /// 最近至多 7 个快照，对 availableBytes 做时间线性拟合；斜率 > 0（正在消耗）时返回
    /// 距水线耗尽的天数；斜率 <= 0 或样本不足 2 返回 nil。
    public func daysUntilFull(snapshots: [Snapshot], waterlineBytes: Int64) -> Double?
}
```

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func predictsDaysUntilFull() {
    let now = Date()
    var snapshots: [Snapshot] = []
    for day in 0..<7 {
        let available = Int64(100_000_000_000 - Int64(day) * 2_000_000_000)
        snapshots.append(Snapshot(
            volume: VolumeInfo(totalBytes: 1_000_000_000_000, availableBytes: available, timestamp: now.addingTimeInterval(Double(day) * 86_400)),
            items: []
        ))
    }
    let days = FullPrediction().daysUntilFull(snapshots: snapshots, waterlineBytes: 30_000_000_000)
    // 7 天线性拟合：第 0 天 100GB、第 6 天 88GB，斜率 -2GB/天；
    // 从最后一个快照（88GB）到水线 30GB：(88-30)/2 = 29 天
    #expect(days != nil)
    #expect(abs((days ?? 0) - 29) < 1.0)
}

@Test func stableDiskReturnsNil() {
    let now = Date()
    let snapshots = (0..<5).map { day in
        Snapshot(
            volume: VolumeInfo(totalBytes: 1000, availableBytes: 900, timestamp: now.addingTimeInterval(Double(day) * 86_400)),
            items: []
        )
    }
    #expect(FullPrediction().daysUntilFull(snapshots: snapshots, waterlineBytes: 100) == nil)
}
```

- [ ] **Step 2: 运行测试确认失败** —— Expected: FAIL。

- [ ] **Step 3: 实现（最小二乘线性拟合）**

```swift
public struct FullPrediction: Sendable {
    public init() {}

    public func daysUntilFull(snapshots: [Snapshot], waterlineBytes: Int64) -> Double? {
        let points = Array(snapshots.suffix(7))
        guard points.count >= 2 else { return nil }
        let x: [Double] = points.enumerated().map { Double($0.offset) }
        let y: [Double] = points.map { Double($0.volume.availableBytes) }
        let n = Double(x.count)
        let sumX = x.reduce(0, +)
        let sumY = y.reduce(0, +)
        let sumXY = zip(x, y).map(*).reduce(0, +)
        let sumX2 = x.map { $0 * $0 }.reduce(0, +)
        let denom = n * sumX2 - sumX * sumX
        guard abs(denom) > 1e-9 else { return nil }
        let slope = (n * sumXY - sumX * sumY) / denom
        guard slope > 0 else { return nil }
        let headroom = Double(points.last!.volume.availableBytes) - Double(waterlineBytes)
        return headroom / slope
    }
}
```

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Flow/FullPrediction.swift Tests/DiskReservoirCoreTests/FullPredictionTests.swift
git commit -m "feat: predict days until disk full from snapshot trend"
```

---

### Task 10: 规则评估器

**Files:**
- Create: `Sources/DiskReservoirCore/Cleaner/ProcessInspecting.swift`
- Create: `Sources/DiskReservoirCore/Cleaner/RuleEvaluator.swift`
- Test: `Tests/DiskReservoirCoreTests/RuleEvaluatorTests.swift`

**Interfaces:**
- Consumes: `Config`、`CleanRule`、`ScanItem`。
- Produces:

```swift
public protocol ProcessInspecting: Sendable {
    func isRunning(_ processName: String) -> Bool
}

public struct AlwaysFalseProcessInspector: ProcessInspecting {
    public init() {}
    public func isRunning(_ processName: String) -> Bool { false }
}

public enum Action: Equatable, Sendable {
    case skip(reason: String)
    case trash
    case delete
    case notify(reason: String)
}

public struct EvaluatedAction: Equatable, Sendable {
    public let itemID: String
    public let action: Action
}

public struct RuleEvaluator: Sendable {
    public init(config: Config, now: @escaping () -> Date = Date.init)
    public func evaluate(item: ScanItem, isProcessRunning: (String?) -> Bool) -> EvaluatedAction
}
```

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

private func item(
    _ id: String, safety: SafetyLevel, disposition: CleanDisposition,
    size: Int64 = 1_000_000_000, modified: Date? = Date()
) -> ScanItem {
    ScanItem(
        id: id, recipeID: "r", name: "N", path: "/tmp/\(id)",
        category: .xcode, safety: safety, disposition: disposition,
        sizeBytes: size, allocatedBytes: size, reclaimableBytes: size,
        fileCount: 1, lastModified: modified
    )
}

@Test func userConfirmAlwaysRequiresConfirmation() {
    let evaluator = RuleEvaluator(config: .default)
    let result = evaluator.evaluate(
        item: item("u", safety: .userConfirm, disposition: .trash),
        isProcessRunning: { _ in false }
    )
    guard case .notify = result.action else { return #expect(Bool(false), "expected notify") }
}

@Test func requiresQuitWithRunningProcessNotifies() {
    let evaluator = RuleEvaluator(config: .default)
    let result = evaluator.evaluate(
        item: item("q", safety: .requiresQuit, disposition: .deletePermanently),
        isProcessRunning: { _ in true }
    )
    guard case .notify = result.action else { return #expect(Bool(false), "expected notify") }
}

@Test func recentlyModifiedIsSkipped() {
    let evaluator = RuleEvaluator(config: .default, now: { Date(timeIntervalSince1970: 1_000_000) })
    let result = evaluator.evaluate(
        item: item("m", safety: .safeWhileRunning, disposition: .deletePermanently,
                   modified: Date(timeIntervalSince1970: 999_000)),
        isProcessRunning: { _ in false }
    )
    guard case .skip = result.action else { return #expect(Bool(false), "expected skip") }
}

@Test func oldEnoughItemIsDeleted() {
    let evaluator = RuleEvaluator(config: .default, now: { Date(timeIntervalSince1970: 1_000_000) })
    let result = evaluator.evaluate(
        item: item("o", safety: .safeWhileRunning, disposition: .deletePermanently,
                   modified: Date(timeIntervalSince1970: 900_000)),
        isProcessRunning: { _ in false }
    )
    #expect(result.action == .delete)
}
```

- [ ] **Step 2: 运行测试确认失败** —— Expected: FAIL。

- [ ] **Step 3: 实现**

```swift
public protocol ProcessInspecting: Sendable {
    func isRunning(_ processName: String) -> Bool
}

public struct AlwaysFalseProcessInspector: ProcessInspecting {
    public init() {}
    public func isRunning(_ processName: String) -> Bool { false }
}

public enum Action: Equatable, Sendable {
    case skip(reason: String)
    case trash
    case delete
    case notify(reason: String)
}

public struct EvaluatedAction: Equatable, Sendable {
    public let itemID: String
    public let action: Action
}

public struct RuleEvaluator: Sendable {
    private let config: Config
    private let now: () -> Date

    public init(config: Config, now: @escaping () -> Date = Date.init) {
        self.config = config
        self.now = now
    }

    public func evaluate(item: ScanItem, isProcessRunning: (String?) -> Bool) -> EvaluatedAction {
        let rule = config.rules.first { $0.recipeID == item.recipeID }
        if config.whitelistPaths.contains(item.path) {
            return EvaluatedAction(itemID: item.id, action: .skip(reason: "whitelisted"))
        }
        if !(rule?.enabled ?? true) {
            return EvaluatedAction(itemID: item.id, action: .skip(reason: "disabled"))
        }
        switch item.safety {
        case .userConfirm:
            return EvaluatedAction(itemID: item.id, action: .notify(reason: "requires user confirmation"))
        case .requiresQuit:
            if let process = itemProcessName(for: item), isProcessRunning(process) {
                return EvaluatedAction(itemID: item.id, action: .notify(reason: "process running: \(process)"))
            }
        case .safeWhileRunning:
            break
        }
        guard let modified = item.lastModified else {
            return EvaluatedAction(itemID: item.id, action: .skip(reason: "no modification date"))
        }
        let ageLimit = Double(rule?.maxAgeDays ?? recipeDefaultAge(for: item))
        let olderThanLimit = now().timeIntervalSince(modified) > ageLimit * 86_400
        let notRecentlyModified = now().timeIntervalSince(modified) > 86_400
        guard olderThanLimit, notRecentlyModified else {
            return EvaluatedAction(itemID: item.id, action: .skip(reason: "too recent"))
        }
        let disposition = rule?.disposition ?? item.disposition
        switch disposition {
        case .trash: return EvaluatedAction(itemID: item.id, action: .trash)
        case .deletePermanently: return EvaluatedAction(itemID: item.id, action: .delete)
        case .none: return EvaluatedAction(itemID: item.id, action: .skip(reason: "disposition none"))
        }
    }

    private func itemProcessName(for item: ScanItem) -> String? {
        // 由注册表映射：xcode 相关 → "Xcode"，simulator → "Simulator"，packageManager → nil
        switch item.category {
        case .xcode: return "Xcode"
        case .simulator: return "Simulator"
        default: return nil
        }
    }

    private func recipeDefaultAge(for item: ScanItem) -> Int {
        switch item.category {
        case .xcode: return item.recipeID == "xctestdevices" ? 3 : 7
        default: return 30
        }
    }
}
```

说明：`requiresQuit` 且进程未运行时，继续走年龄/处置判断（与测试一致）。

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Cleaner Tests/DiskReservoirCoreTests/RuleEvaluatorTests.swift
git commit -m "feat: rule evaluator with safety levels and recency guard"
```

---

### Task 11: 进程检测（pgrep）

**Files:**
- Create: `Sources/DiskReservoirCore/Cleaner/PGrepProcessInspector.swift`
- Test: `Tests/DiskReservoirCoreTests/PGrepProcessInspectorTests.swift`

**Interfaces:**
- Consumes: `ProcessInspecting`。
- Produces:

```swift
public struct PGrepProcessInspector: ProcessInspecting {
    public init(pgrepURL: URL = URL(fileURLWithPath: "/usr/bin/pgrep"), maxAttempts: Int = 3)
    public func isRunning(_ processName: String) -> Bool
}
```

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func pgrepDetectsCurrentShellProcess() throws {
    // 用当前进程名做正向检测：pgrep -x <自身名> 应至少匹配自己
    let name = ProcessInfo.processInfo.processName
    let inspector = PGrepProcessInspector()
    let running = inspector.isRunning(name)
    // 进程名可能被截断，因此允许 false，但绝不抛错
    _ = running
}

@Test func pgrepReturnsFalseForImpossibleName() {
    let inspector = PGrepProcessInspector()
    #expect(!inspector.isRunning("pp-definitely-not-running-\(UUID().uuidString)"))
}
```

- [ ] **Step 2: 运行测试确认失败** —— Expected: FAIL（类型不存在）。

- [ ] **Step 3: 实现**

```swift
import Foundation

public struct PGrepProcessInspector: ProcessInspecting {
    private let pgrepURL: URL
    private let maxAttempts: Int

    public init(pgrepURL: URL = URL(fileURLWithPath: "/usr/bin/pgrep"), maxAttempts: Int = 3) {
        self.pgrepURL = pgrepURL
        self.maxAttempts = maxAttempts
    }

    public func isRunning(_ processName: String) -> Bool {
        for _ in 0..<maxAttempts {
            let process = Process()
            process.executableURL = pgrepURL
            process.arguments = ["-x", processName]
            let pipe = Pipe()
            process.standardOutput = pipe
            process.standardError = Pipe()
            do {
                try process.run()
                process.waitUntilExit()
                if process.terminationStatus == 0 { return true }
            } catch {
                return false
            }
        }
        return false
    }
}
```

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Cleaner/PGrepProcessInspector.swift Tests/DiskReservoirCoreTests/PGrepProcessInspectorTests.swift
git commit -m "feat: pgrep-based process inspector"
```

---

### Task 12: 文件删除器

**Files:**
- Create: `Sources/DiskReservoirCore/Cleaner/FileDeleting.swift`
- Test: `Tests/DiskReservoirCoreTests/FileDeleterTests.swift`

**Interfaces:**
- Consumes: `CleanDisposition`。
- Produces:

```swift
public protocol FileDeleting: Sendable {
    func delete(url: URL, disposition: CleanDisposition) throws -> Int64
}

public struct FileManagerFileDeleter: FileDeleting {
    public init(fileManager: FileManager = .default)
    public func delete(url: URL, disposition: CleanDisposition) throws -> Int64
}
```

`delete` 返回释放的字节数（删除前计算目录 allocated 大小）。`trash` 走 `FileManager.trashItem`，`deletePermanently` 走 `removeItem`。

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

@Test func permanentDeleteRemovesDirectory() throws {
    let root = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-del-\(UUID().uuidString)", isDirectory: true)
    try FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
    try Data(repeating: 7, count: 4096).write(to: root.appendingPathComponent("f.bin"))
    let freed = try FileManagerFileDeleter().delete(url: root, disposition: .deletePermanently)
    #expect(!FileManager.default.fileExists(atPath: root.path))
    #expect(freed > 0)
}

@Test func noneDispositionDoesNothing() throws {
    let root = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-del2-\(UUID().uuidString)", isDirectory: true)
    try FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
    let freed = try FileManagerFileDeleter().delete(url: root, disposition: .none)
    #expect(FileManager.default.fileExists(atPath: root.path))
    #expect(freed == 0)
}
```

- [ ] **Step 2: 运行测试确认失败** —— Expected: FAIL。

- [ ] **Step 3: 实现**

```swift
public struct FileManagerFileDeleter: FileDeleting {
    private let fileManager: FileManager

    public init(fileManager: FileManager = .default) {
        self.fileManager = fileManager
    }

    public func delete(url: URL, disposition: CleanDisposition) throws -> Int64 {
        switch disposition {
        case .none:
            return 0
        case .trash:
            let allocated = allocatedBytes(of: url)
            var resulting: NSURL?
            try fileManager.trashItem(at: url, resultingItemURL: &resulting)
            return allocated
        case .deletePermanently:
            let allocated = allocatedBytes(of: url)
            try fileManager.removeItem(at: url)
            return allocated
        }
    }

    private func allocatedBytes(of url: URL) -> Int64 {
        guard let enumerator = fileManager.enumerator(
            at: url, includingPropertiesForKeys: [.totalFileAllocatedSizeKey],
            options: [.skipsHiddenFiles]
        ) else { return 0 }
        var total: Int64 = 0
        while let element = enumerator.nextObject() as? URL {
            let values = try? element.resourceValues(forKeys: [.totalFileAllocatedSizeKey])
            total += Int64(values?.totalFileAllocatedSize ?? 0)
        }
        return total
    }
}
```

真实回收站测试会污染用户回收站，因此 `trash` 分支在单测中用 `MockFileDeleter`（见 Task 13 测试）验证协议行为；`FileManagerFileDeleter.trash` 仅由 CLI 手工验证。

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Cleaner/FileDeleting.swift Tests/DiskReservoirCoreTests/FileDeleterTests.swift
git commit -m "feat: file deleter with trash and permanent deletion"
```

---

### Task 13: 清理引擎（水线驱动）

**Files:**
- Create: `Sources/DiskReservoirCore/Cleaner/Cleaner.swift`
- Create: `Sources/DiskReservoirCore/Snapshot/CleanLogStore.swift`
- Test: `Tests/DiskReservoirCoreTests/CleanerTests.swift`

**Interfaces:**
- Consumes: `ScanResult`、`Config`、`RuleEvaluator`、`FileDeleting`、`ProcessInspecting`、`CleanLogEntry`、`StoragePaths`、`JSONStoring`。
- Produces:

```swift
public struct CleanOutcome: Equatable, Sendable {
    public let entries: [CleanLogEntry]
    public let freedBytes: Int64
    public let stillBelowWaterline: Bool
}

public struct CleanLogStore: Sendable {
    public init(paths: StoragePaths, store: JSONStoring = JSONStore())
    public func entries() throws -> [CleanLogEntry]
    public func append(_ entry: CleanLogEntry) throws
    public func prune(retainingDays: Int = 90) throws
}

public struct Cleaner: Sendable {
    public init(
        evaluator: RuleEvaluator,
        deleter: FileDeleting,
        inspector: ProcessInspecting,
        logStore: CleanLogStore,
        now: @escaping () -> Date = Date.init
    )
    /// 当 available < waterlineBytes 时触发；按 lastModified 从旧到新清理，直到回到水线或无可清理项。
    public func run(scan: ScanResult, config: Config, waterlineBytes: Int64) throws -> CleanOutcome
}
```

- [ ] **Step 1: 写失败测试**

```swift
import Testing
import Foundation
@testable import DiskReservoirCore

private struct MockDeleter: FileDeleting {
    func delete(url: URL, disposition: CleanDisposition) throws -> Int64 { 1024 }
}

@Test func cleanerStopsAtWaterline() throws {
    let dir = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-clean-\(UUID().uuidString)", isDirectory: true)
    defer { try? FileManager.default.removeItem(at: dir) }
    let paths = StoragePaths(baseURL: dir)
    let logStore = CleanLogStore(paths: paths)
    let now = Date(timeIntervalSince1970: 1_000_000)
    let item = ScanItem(
        id: "old", recipeID: "xctestdevices", name: "XCTest", path: "/tmp/old",
        category: .xcode, safety: .safeWhileRunning, disposition: .deletePermanently,
        sizeBytes: 5_000_000_000, allocatedBytes: 5_000_000_000, reclaimableBytes: 5_000_000_000,
        fileCount: 1, lastModified: now.addingTimeInterval(-10 * 86_400)
    )
    let scan = ScanResult(
        volume: VolumeInfo(totalBytes: 100_000_000_000, availableBytes: 20_000_000_000, timestamp: now),
        items: [item], records: []
    )
    let cleaner = Cleaner(
        evaluator: RuleEvaluator(config: .default, now: { now }),
        deleter: MockDeleter(),
        inspector: AlwaysFalseProcessInspector(),
        logStore: logStore,
        now: { now }
    )
    let outcome = try cleaner.run(scan: scan, config: .default, waterlineBytes: 30_000_000_000)
    // 可释放 5GB，缺口 10GB → 清理后仍低于水线
    #expect(outcome.freedBytes == 1024)
    #expect(outcome.stillBelowWaterline == true)
    #expect(try logStore.entries().count == 1)
}
```

- [ ] **Step 2: 运行测试确认失败** —— Expected: FAIL。

- [ ] **Step 3: 实现**

`CleanLogStore.swift`（与 SnapshotStore 同模式，文件 `cleanLogURL`）：

```swift
public struct CleanLogStore: Sendable {
    private let paths: StoragePaths
    private let store: JSONStoring

    public init(paths: StoragePaths, store: JSONStoring = JSONStore()) {
        self.paths = paths
        self.store = store
    }

    public func entries() throws -> [CleanLogEntry] {
        try store.load([CleanLogEntry].self, from: paths.cleanLogURL) ?? []
    }

    public func append(_ entry: CleanLogEntry) throws {
        var all = try entries()
        all.append(entry)
        try store.save(all, to: paths.cleanLogURL)
    }

    public func prune(retainingDays: Int = 90) throws {
        let cutoff = Date().addingTimeInterval(-Double(retainingDays) * 86_400)
        let kept = try entries().filter { $0.timestamp >= cutoff }
        try store.save(kept, to: paths.cleanLogURL)
    }
}
```

`Cleaner.swift`：

```swift
public struct CleanOutcome: Equatable, Sendable {
    public let entries: [CleanLogEntry]
    public let freedBytes: Int64
    public let stillBelowWaterline: Bool

    public init(entries: [CleanLogEntry], freedBytes: Int64, stillBelowWaterline: Bool) {
        self.entries = entries
        self.freedBytes = freedBytes
        self.stillBelowWaterline = stillBelowWaterline
    }
}

public struct Cleaner: Sendable {
    private let evaluator: RuleEvaluator
    private let deleter: FileDeleting
    private let inspector: ProcessInspecting
    private let logStore: CleanLogStore
    private let now: () -> Date

    public init(
        evaluator: RuleEvaluator, deleter: FileDeleting, inspector: ProcessInspecting,
        logStore: CleanLogStore, now: @escaping () -> Date = Date.init
    ) {
        self.evaluator = evaluator
        self.deleter = deleter
        self.inspector = inspector
        self.logStore = logStore
        self.now = now
    }

    public func run(scan: ScanResult, config: Config, waterlineBytes: Int64) throws -> CleanOutcome {
        var deficit = waterlineBytes - scan.volume.availableBytes
        guard deficit > 0 else {
            return CleanOutcome(entries: [], freedBytes: 0, stillBelowWaterline: false)
        }
        let candidates = scan.items
            .filter { !config.whitelistPaths.contains($0.path) }
            .sorted { ($0.lastModified ?? .distantPast) < ($1.lastModified ?? .distantPast) }
        var entries: [CleanLogEntry] = []
        var freedTotal: Int64 = 0
        var below = true
        for item in candidates {
            guard deficit > 0 else { below = false; break }
            let decision = evaluator.evaluate(item: item) { name in
                name.map { inspector.isRunning($0) } ?? false
            }
            let disposition: CleanDisposition?
            switch decision.action {
            case .delete: disposition = .deletePermanently
            case .trash: disposition = .trash
            default: disposition = nil
            }
            guard let disposition else { continue }
            let freed = try deleter.delete(url: URL(fileURLWithPath: item.path), disposition: disposition)
            freedTotal += freed
            deficit -= freed
            entries.append(CleanLogEntry(
                id: UUID(), timestamp: now(), itemIDs: [item.id],
                freedBytes: freed, disposition: disposition
            ))
        }
        if entries.isEmpty {
            below = scan.volume.availableBytes < waterlineBytes
        } else if deficit > 0 {
            below = true
        }
        for entry in entries {
            try logStore.append(entry)
        }
        return CleanOutcome(entries: entries, freedBytes: freedTotal, stillBelowWaterline: below)
    }
}
```

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add Sources/DiskReservoirCore/Cleaner/Cleaner.swift Sources/DiskReservoirCore/Snapshot/CleanLogStore.swift Tests/DiskReservoirCoreTests/CleanerTests.swift
git commit -m "feat: waterline-driven cleaner with clean log"
```

---

### Task 14: CLI 骨架与 JSON 输出

**Files:**
- Modify: `Sources/poolproblem/PoolProblemCLI.swift`
- Create: `Sources/poolproblem/JSONOutput.swift`
- Create: `Sources/poolproblem/Commands/ScanCommand.swift`
- Create: `Sources/poolproblem/Commands/SuggestCommand.swift`
- Create: `Sources/poolproblem/Commands/CleanCommand.swift`
- Create: `Sources/poolproblem/Commands/StatusCommand.swift`
- Test: `Tests/PoolProblemCLITests/CLIEndToEndTests.swift`

**Interfaces:**
- Consumes: `DiskReservoirCore` 全部公共类型。
- Produces: `poolproblem` 四个子命令。

- [ ] **Step 1: 写失败测试（端到端）**

```swift
import Testing
import Foundation

@Test func cliScanOutputsJSON() throws {
    let dataDir = FileManager.default.temporaryDirectory
        .appendingPathComponent("pp-cli-\(UUID().uuidString)", isDirectory: true)
    try FileManager.default.createDirectory(at: dataDir, withIntermediateDirectories: true)
    defer { try? FileManager.default.removeItem(at: dataDir) }
    setenv("POOLPROBLEM_DATA_DIR", dataDir.path, 1)
    defer { unsetenv("POOLPROBLEM_DATA_DIR") }

    let process = Process()
    process.executableURL = productsDirectory.appendingPathComponent("poolproblem")
    process.arguments = ["scan", "--json"]
    let pipe = Pipe()
    process.standardOutput = pipe
    process.standardError = Pipe()
    try process.run()
    process.waitUntilExit()
    let data = pipe.fileHandleForReading.readDataToEndOfFile()
    let object = try JSONSerialization.jsonObject(with: data) as? [String: Any]
    #expect(process.terminationStatus == 0)
    #expect(object?["version"] as? Int == 1)
    #expect(object?["volume"] is [String: Any])
    #expect(object?["items"] is [[String: Any]])
}

private var productsDirectory: URL {
    // Tests/PoolProblemCLITests/CLIEndToEndTests.swift → 上溯 3 级到包根目录
    let fileURL = URL(fileURLWithPath: #filePath)
    let packageRoot = fileURL
        .deletingLastPathComponent()
        .deletingLastPathComponent()
        .deletingLastPathComponent()
    return packageRoot.appendingPathComponent(".build/debug", isDirectory: true)
}
```

- [ ] **Step 2: 运行测试确认失败** —— Run: `swift test --filter CLIEndToEndTests`，Expected: FAIL（命令不存在）。

- [ ] **Step 3: 实现 CLI**

`JSONOutput.swift`：

```swift
import DiskReservoirCore
import Foundation

struct JSONOutput {
    static func scan(result: ScanResult) throws -> Data {
        let payload: [String: Any] = [
            "version": DiskReservoirCore.schemaVersion,
            "timestamp": ISO8601DateFormatter().string(from: result.volume.timestamp),
            "volume": [
                "totalBytes": result.volume.totalBytes,
                "availableBytes": result.volume.availableBytes,
            ],
            "items": result.items.map { item in
                [
                    "id": item.id, "recipeID": item.recipeID, "name": item.name,
                    "path": item.path, "category": item.category.rawValue,
                    "safety": item.safety.rawValue, "disposition": item.disposition.rawValue,
                    "sizeBytes": item.sizeBytes, "allocatedBytes": item.allocatedBytes,
                    "reclaimableBytes": item.reclaimableBytes, "fileCount": item.fileCount,
                ]
            },
        ]
        return try JSONSerialization.data(withJSONObject: payload, options: [.prettyPrinted, .sortedKeys])
    }
}
```

`ScanCommand.swift`：

```swift
import ArgumentParser
import DiskReservoirCore
import Foundation

struct ScanCommand: ParsableCommand {
    static let configuration = CommandConfiguration(
        commandName: "scan", abstract: "扫描各配方并输出大小与可释放量"
    )
    @Flag(name: .long, help: "输出 JSON")
    var json = false

    func run() throws {
        let paths = StoragePaths()
        let scanner = Scanner()
        let result = try scanner.scan(recipes: RecipeRegistry.builtIn(), homeDirectory: NSHomeDirectory())
        if json {
            let data = try JSONOutput.scan(result: result)
            FileHandle.standardOutput.write(data)
            FileHandle.standardOutput.write(Data("\n".utf8))
        } else {
            print("可用: \(result.volume.availableBytes) 字节")
            for item in result.items.sorted(by: { $0.reclaimableBytes > $1.reclaimableBytes }) {
                print("\(item.name): 可释放 \(item.reclaimableBytes) 字节 (\(item.safety.rawValue))")
            }
        }
    }
}
```

`SuggestCommand.swift`：调用 `Scanner` + `RuleEvaluator`，输出建议项（JSON：`{ "version":1, "suggestions":[{"itemID":..., "action":..., "reason":...}] }`）。`CleanCommand.swift`：`--dry-run` 只预览；否则执行 `Cleaner`，JSON 输出 `{ "version":1, "freedBytes":..., "entries":[...] }`。`StatusCommand.swift`：读取 `SnapshotStore` + `CleanLogStore`，输出水位、`FullPrediction` 结果与最近 10 条清理日志（JSON 或可读文本）。

`PoolProblemCLI.swift` 增加 `subcommands`：

```swift
static let configuration = CommandConfiguration(
    commandName: "poolproblem",
    abstract: "The Pool Problem - 磁盘蓄水池：扫描、预测、清理。",
    version: "0.1.0",
    subcommands: [ScanCommand.self, SuggestCommand.self, CleanCommand.self, StatusCommand.self]
)
```

- [ ] **Step 4: 运行测试确认通过** —— Expected: PASS。

- [ ] **Step 5: 手工冒烟验证**

Run: `swift run poolproblem scan --json`

Expected: 输出合法 JSON，包含 `volume` 与 `items` 数组。

- [ ] **Step 6: 提交**

```bash
git add Sources/poolproblem Tests/PoolProblemCLITests
git commit -m "feat: CLI commands with stable JSON output"
```

---

### Task 15: README 与使用说明

**Files:**
- Create: `README.md`

- [ ] **Step 1: 写 README**

内容：项目简介（The Pool Problem / 蓄水池模型一句话）、当前状态（M1/M2 完成）、构建方式（`swift build`）、测试（`swift test`）、CLI 用法示例（scan/suggest/clean --dry-run/status）、数据目录说明（`POOLPROBLEM_DATA_DIR`）、未来计划（App/小组件/MCP）。

- [ ] **Step 2: 提交**

```bash
git add README.md
git commit -m "docs: add README with build and CLI usage"
```

---

## Self-Review 记录

- **Spec 覆盖**：M1（配方/扫描/快照/流速/预测/清理/进程锁）→ Task 2-13；M2（CLI scan/suggest/clean/status + JSON）→ Task 14；诚实计量（APFS 感知）→ Task 6；水线守护 → Task 13；错误处理（JSONStore 缺失返回 nil、扫描跳过不存在路径、pgrep 失败返回 false）→ 相应任务；测试策略 → 每任务 TDD。
- **待后续计划**（不在本计划）：App（M3）、小组件（M4）、打包（M5）、FSEvents 源头治理（依赖 M3 常驻进程）、App Group 实际配置（M3 时随 Xcode 工程配置）、Sparkle（M5）。
- **占位符扫描**：无 TBD/TODO；所有步骤含实际代码。
- **类型一致性**：`StoragePaths` 在 Task 4 后带 `homeDirectory` 成员与 `init(baseURL:homeDirectory:)`；Task 3 测试调用已同步修改；`Cleaner` 测试中 `MockDeleter` 以不可变 struct 实现（`delete` 返回固定 1024），避免值语义陷阱；CLI 端到端测试通过 `#filePath` 定位 `.build/debug/poolproblem`。
